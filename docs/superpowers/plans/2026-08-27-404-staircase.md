# 404 Dynamic Staircase Implementation Plan

> For agentic workers: REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

Goal: 将上游提交 403f59b3dc1fc7a9e2abee79ca5b74be59c66057 的动态 404 楼梯场景覆盖合入当前 master 工作区。

Architecture: 直接以目标提交中的五个文件版本作为合入结果，避免把当前分支的大量首页改动反向合并。404 页面负责结构和生命周期，not-found-scene.ts 负责 Canvas 与数字物理动画，CSS 负责视觉布局和响应式表现。

Tech Stack: Astro 7、TypeScript、CSS、Canvas 2D、Vitest、pnpm。

## Global Constraints

- 仅覆盖目标提交修改的五个文件以及本次流程文档。
- src/constants/icons.ts 以目标提交版本为准，丢弃其中当前未提交的本地修改。
- 不创建或推送 Git commit。
- 保留现有国际化文案、站点路由和 Astro 页面切换支持。

---

### Task 1: 记录当前状态并覆盖目标文件

Files:
- Modify: src/constants/icons.ts
- Modify: src/pages/404.astro
- Modify: src/styles/main.css
- Modify: src/styles/pages/404.css
- Create: src/utils/not-found-scene.ts

Interfaces:
- Consumes: Git object 403f59b3dc1fc7a9e2abee79ca5b74be59c66057 and its tree.
- Produces: 404 页面组件调用 mountNotFoundScene(root)，场景模块返回清理函数。

- [ ] 保存当前 Git 状态，确认只有已获授权覆盖的 src/constants/icons.ts 未提交改动。
- [ ] 使用目标提交的文件版本覆盖五个目标文件，不执行全分支 reset，不影响其他文件。
- [ ] 检查 git diff --name-only，确认文件范围仅为目标文件和流程文档。

### Task 2: 静态与类型验证

Files:
- Test: src/pages/404.astro
- Test: src/utils/not-found-scene.ts

Interfaces:
- Consumes: Task 1 的页面结构和场景导出。
- Produces: 可通过 Astro 类型检查的 404 实现。

- [ ] 运行 pnpm exec astro check，记录完整退出码和错误数。
- [ ] 运行 pnpm exec tsc --noEmit，确认场景模块和页面脚本类型正确。

### Task 3: 运行回归测试与构建

Files:
- Test: src/**/*.test.ts

Interfaces:
- Consumes: Task 2 通过的页面和场景代码。
- Produces: 现有测试和生产构建验证结果。

- [ ] 运行 pnpm test，确认现有 Vitest 测试结果。
- [ ] 运行 pnpm exec astro build，确认生产构建结果；若脚本在环境依赖步骤失败，记录具体命令和错误。
- [ ] 复查工作区 diff，确认未产生冲突标记、未覆盖目标范围外的源文件，并汇报验证结果。
